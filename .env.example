# Palladium Configuration
# CRITICAL: Change ALL passwords before connecting to the internet!
# Run setup.sh to generate secure credentials.

# Default timezone
TZ=UTC

# PostgreSQL (used by many services)
POSTGRES_USER=admin
POSTGRES_PASSWORD=changeme
POSTGRES_DB=defaultdb

# Default port range for auto-deployed services
PUBLIC_PORT_BASE=8000
