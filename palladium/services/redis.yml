# desc: Redis cache and message broker
version: "3.8"

services:
  redis:
    image: redis:alpine
    container_name: redis
    restart: unless-stopped
    ports:
      - "${PORT}:6379"
    volumes:
      - ./data:/data
